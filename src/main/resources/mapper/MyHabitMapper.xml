<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.backend.Myhabit.mapper.MyHabitMapper">

    <insert id="insertMyHabit" parameterType="com.example.backend.Myhabit.vo.MyHabitVO">
        INSERT INTO MyHabit (user_id, habit_id2, state)
        VALUES (#{userId}, #{habitId2}, #{state})
    </insert>

    <select id="selectMyHabitById" parameterType="long" resultType="com.example.backend.Myhabit.vo.MyHabitVO">
        SELECT * FROM MyHabit WHERE my_habit_id = #{myHabitId}
    </select>

    <select id="selectAllMyHabits" resultType="com.example.backend.Myhabit.vo.MyHabitVO">
        SELECT * FROM MyHabit
    </select>

    <update id="updateMyHabit" parameterType="com.example.backend.Myhabit.vo.MyHabitVO">
        UPDATE MyHabit
        SET user_id = #{userId}, habit_id2 = #{habitId2}, state = #{state}
        WHERE my_habit_id = #{myHabitId}
    </update>

    <delete id="deleteMyHabit" parameterType="long">
        DELETE FROM MyHabit WHERE my_habit_id = #{myHabitId}
    </delete>
    <!-- 대기 상태 (OFF)인 습관 목록 조회 -->
    <select id="selectHabitsByStateOff" resultType="com.example.backend.Myhabit.vo.MyHabitVO">
        SELECT h.habit_title, h.category_title
        FROM MyHabit mh
                 JOIN Habit h ON mh.habit_id = h.habit_id
        WHERE mh.state = 'OFF';
    </select>

    <!-- 진행 상태 (ON)인 습관 목록 조회 -->
    <select id="selectHabitsByStateOn" resultType="com.example.backend.Myhabit.vo.MyHabitVO">
        SELECT h.habit_title, h.category_title
        FROM MyHabit mh
                 JOIN Habit h ON mh.habit_id = h.habit_id
        WHERE mh.state = 'ON';
    </select>

    <!-- 습관 상태 'ON'으로 변경 -->
    <update id="updateHabitStateOn">
        UPDATE MyHabit
        SET state = 'ON'
        WHERE my_habit_id = #{myHabitId};
    </update>

    <!-- 습관 상태 'OFF'로 변경 -->
    <update id="updateHabitStateOff">
        UPDATE MyHabit
        SET state = 'OFF'
        WHERE my_habit_id = #{myHabitId};
    </update>
    <!-- 진행중인 습관 제목을 가져오는 쿼리 -->
    <select id="selectHabitTitlesByState" resultType="String">
        SELECT h.habit_title
        FROM MyHabit mh
                 JOIN Habit h ON mh.habit_id = h.habit_id
        WHERE mh.state = 'on'
    </select>
</mapper>
